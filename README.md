# Portainer Setups

Use:
```bash
INVOL=/path/to/appdata/portainer
git clone https://github.com/FinchTechSoCal/docker-portainer.git $INVOL
cd $INVOL
```

Modify .env, then
```bash
docker compose up -d
```